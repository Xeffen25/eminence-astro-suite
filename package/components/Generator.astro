---
import config from "virtual:eminence-astro-suite/config";

interface Props {
	/**
	 * Enables generator meta tag rendering.
	 * @default true
	 */
	generate?: boolean;
}

const generate = Astro.props.generate ?? config.generator ?? true;
---

{generate && <meta name="generator" content={Astro.generator} />}
