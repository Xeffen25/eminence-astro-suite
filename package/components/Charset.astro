---
interface Props {
	/**
	 * Character encoding used in the rendered meta tag.
	 * @default "utf-8"
	 */
	charset?: string;
}

const { charset = "utf-8" } = Astro.props;
---

<meta charset={charset} />
