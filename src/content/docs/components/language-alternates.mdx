---
title: LanguageAlternates - Component
description: Documentation for LanguageAlternates, focused on rendering language-specific rel="alternate" link tags. It handles hreflang entries from language-code-to-URL mappings.
---

import { Code } from "@astrojs/starlight/components";
import LanguageAlternatesSource from "@package/components/LanguageAlternates.astro?raw";

LanguageAlternates renders one or more `<link rel="alternate">` tags with `hreflang` values for localized or regional URL variants.

## Props

| prop        | type                            | default | required | description                                                                                      |
| ----------- | ------------------------------- | ------- | -------- | ------------------------------------------------------------------------------------------------ |
| `languages` | `Record<string, string \| URL>` | -       | Yes      | Map of `hreflang` values (for example `es`, `fr`, `x-default`) to URL values rendered as `href`. |

## Usage & Examples

**Input**

```astro
<LanguageAlternates
	languages={{
		es: "https://example.com/es",
		fr: new URL("https://example.com/fr"),
		"x-default": "https://example.com",
	}}
/>
```

**Output**

```html
<link rel="alternate" hreflang="es" href="https://example.com/es" />
<link rel="alternate" hreflang="fr" href="https://example.com/fr" />
<link rel="alternate" hreflang="x-default" href="https://example.com" />
```

## Decisions Made

This component only handles language alternates. Feed alternates like RSS or Atom are intentionally outside this component's responsibility. This was made to keep concerns separated from feed discovery metadata and reduce prop-surface overlap between components.

## Source code

<Code code={LanguageAlternatesSource} lang="astro" />

## Reference

- [MDN: rel="alternate" attribute reference](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/rel#alternate)
- [MDN: hreflang global guidance via alternate links](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/link#hreflang)
- [Google Search Central: Tell Google about localized versions of your page](https://developers.google.com/search/docs/specialty/international/localized-versions)
