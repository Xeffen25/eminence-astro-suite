---
title: Manifest
description: Astro component that renders the web app manifest link tag. Supports explicit URLs and integration-config fallback for PWA metadata.
head:
  - tag: title
    content: Manifest - Component
---

import { Code } from "@astrojs/starlight/components";
import ManifestSource from "@package/components/Manifest.astro?raw";

Manifest renders a `<link rel="manifest">` tag that points browsers to your web app manifest file. It only resolves and outputs the href for that tag and does not generate the manifest file itself.

## Props

| prop   | type            | default                         | required | description                                                                                                                           |
| ------ | --------------- | ------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `href` | `string \| URL` | derived from integration config | No       | Absolute URL for the manifest file. When omitted, falls back to integration config (`headTags.manifest` resolved by the integration). |

## Examples

### Basic

Input:

```astro
<Manifest href="https://example.com/manifest.webmanifest" />
```

Output:

```html
<link rel="manifest" href="https://example.com/manifest.webmanifest" />
```

### Automatic

Uses the integration-resolved default when `headTags.manifest` is enabled or a `manifest` config is provided.

Input:

```js
// astro.config.mjs
eminence({
  headTags: {
    manifest: true,
  },
});
```

or with explicit manifest config. See [Manifest Integration](./integration/manifest) for details on config options.

```astro
<Manifest />
```

Output (assuming `Astro.site` is `https://example.com`):

```html
<link rel="manifest" href="https://example.com/manifest.webmanifest" />
```

## Decisions Made

### Integration config resolves automatic defaults

This component does not build manifest URLs itself. Automatic behavior is resolved upstream in the integration virtual config, then consumed here.

### Renders nothing when no href is available

If neither the prop nor integration config provides a truthy href, the component emits no tag.

## Source code

<Code code={ManifestSource} lang="astro" />

## Reference

- [MDN: rel="manifest"](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/rel/manifest)
- [W3C Web App Manifest Specification](https://www.w3.org/TR/appmanifest/)
- [Google Web Fundamentals: Add a Web App Manifest](https://web.dev/add-manifest/)
