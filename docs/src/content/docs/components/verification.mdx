---
title: Verification
description: Astro component that renders site ownership verification meta tags for search providers. Manages Google, Yandex, Bing, and custom provider tokens.
head:
  - tag: title
    content: Verification - Component
---

import { Code } from "@astrojs/starlight/components";
import VerificationSource from "@package/components/Verification.astro?raw";

Verification renders `<meta>` tags that prove site ownership to search engines and webmaster tools. It is scoped to emitting those verification tags only and does not interact with crawling or indexing behavior directly.

## Props

| prop     | type                                  | default | required | description                                                                                                             |
| -------- | ------------------------------------- | ------- | -------- | ----------------------------------------------------------------------------------------------------------------------- |
| `google` | `string`                              | -       | No       | Google Search Console verification token. Renders as `<meta name="google-site-verification">`.                          |
| `yandex` | `string`                              | -       | No       | Yandex Webmaster Tools verification token. Renders as `<meta name="yandex-verification">`.                              |
| `bing`   | `string`                              | -       | No       | Bing (and Yahoo) Webmaster Tools verification token. Renders as `<meta name="msvalidate.01">`.                          |
| `others` | `{ name: string; content: string }[]` | -       | No       | Additional verification entries for other providers. Each entry renders a `<meta>` tag with the given name and content. |

## Examples

### Basic

Input:

```astro
<Verification google="abc123" />
```

Output:

```html
<meta name="google-site-verification" content="abc123" />
```

### Automatic

Uses integration `headTags.verification` when no props are provided.

Input:

```astro
<Verification />
```

Output (when integration config has `headTags.verification: { google: "abc123" }`):

```html
<meta name="google-site-verification" content="abc123" />
```

### Complete usage

Input:

```astro
<Verification
  google="google-token"
  yandex="yandex-token"
  bing="bing-token"
  others={[{ name: "p:domain_verify", content: "pinterest-token" }]}
/>
```

Output:

```html
<meta name="google-site-verification" content="google-token" />
<meta name="yandex-verification" content="yandex-token" />
<meta name="msvalidate.01" content="bing-token" />
<meta name="p:domain_verify" content="pinterest-token" />
```

## Decisions Made

### The others prop handles any additional provider

Built-in props cover the most common search engines. Providers outside that set, such as Pinterest (`p:domain_verify`) or Naver (`naver-site-verification`), can be added through the `others` array without requiring component changes.

### Integration config can define global verification tokens

When no props are provided, the component falls back to `headTags.verification`. This allows tokens to be configured once in the integration and inherited across all pages.

## Source code

<Code code={VerificationSource} lang="astro" />

## Reference

- [Google Search Console: site verification](https://developers.google.com/search/docs/crawling-indexing/verify-site-owner)
- [Yandex Webmaster: site verification](https://yandex.com/support/webmaster/service/add-and-verify-site.html)
- [Bing Webmaster Tools: site verification](https://www.bing.com/webmasters/help/how-to-verify-ownership-of-your-site-afcfefc6)
- [MDN: meta element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta)
